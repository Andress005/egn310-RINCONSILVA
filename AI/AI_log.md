#For the containers in each case given we will explain the recommendation of chat GPT and why I  agree or disagredd with this statement.

1.Class roster
AI: list
My choice: a list since the students list is bulnerable to changes and we may want to modify it in the future
I agree with the AI recommendation because a class roster can change over time. Students may be added or removed from the class, so a list is useful because it can be modified when needed. A list also keeps the students in an organized order.

2.Visitor IDs
AI: Set
My choice: since we need the visitors IDs to be unique
I agree with the AI recommendation because visitor IDs should be unique. A set is appropriate because it does not allow duplicate values. This makes it easier to make sure that the same visitor ID is not stored more than once.

3.Student IDs
AI: Dictionary
My choice: To be able to do a student lookup by their ID# we need a dictionary to be able to enter a value (name) per each ID#
I agree with the AI recommendation because a dictionary is designed to store key-value pairs. The student ID can be used as the key and the student's name can be stored as the value. This makes it easy to find a student's name when their ID is provided.

4.Weekday labels
AI: Tuple
My choice: I chose a tuple because I consider the weekday labels a fixed collection that should not be modified.
I agree with the AI recommendation because the weekday labels are a fixed collection that normally should not be changed. A tuple is appropriate because its values cannot be modified after it is created. This helps protect the weekday labels from accidental changes.